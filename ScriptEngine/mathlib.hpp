#pragma once

#include "module.hpp"

/* fast math standard lib
 * The implementations given here are used instead of those in math.nrt.
 */

namespace lang{
	struct MathModule : public NaReTi::Module
	{
		MathModule();
	private:
	};
}